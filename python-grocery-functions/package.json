{
  "scripts": {
    "lint": "mypy",
    "test": "SCRAPER_FEED_HANDLED_TOPIC_ARN=TEST python -m unittest discover",
    "predeploy-sls": "./package.sh",
    "deploy-sls": "sls deploy"
  },
  "dependencies": {
    "serverless": "^3",
    "serverless-cloudformation-sub-variables": "^0.2.1"
  },
  "devDependencies": {
    "serverless-offline": "^14.4.0"
  }
}
